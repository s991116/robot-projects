#ifndef DIRECTIONENUM_H
#define	DIRECTIONENUM_H

#include <string>
#include <map>

enum class DirectionEnum {
    Forward,
    Backwards,
    Left,
    Right,
    Stop,
};

#endif	/* DIRECTIONENUM_H */